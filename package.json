{
  "name": "satmoggle",
  "version": "1.0.0",
  "description": "Competitive SAT practice web application with real-time multiplayer, Bomb Party elimination, and College Board Digital SAT simulation.",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "node backend/src/server.js",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test:backend": "npm test --prefix backend"
  },
  "author": "Antigravity & User",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
